"""Feature engineering utilities.

Currently hosts `derivations` — time-window derivative operators (delta,
ratio, incremental, velocity) and their deterministic recipe serialization
for deploy-time replay.
"""
